{
  "name": "d3.fantasticcoding.com",
  "private": true,
  "scripts": {
    "start": "browser-sync start --server app --files 'app/*.html, app/js/*, app/css/*'"
  },
  "engines": {
    "node": ">=0.10.0"
  },
  "devDependencies": {
    "browser-sync": "^2.11.1"
  }
}
